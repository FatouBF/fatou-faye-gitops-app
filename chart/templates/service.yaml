apiVersion: v1
kind: Service
metadata:
  name: fatou-faye-release-svc
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: fatou-faye-app
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
